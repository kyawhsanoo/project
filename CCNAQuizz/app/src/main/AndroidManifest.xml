<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.project.my.ccnaquizz" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
         >

        <activity
            android:name=".user.Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.Registration"

            android:label="Registration"
            ></activity>

        <activity android:name=".main.CCNAQuizList"

            android:label="CCNA QUIZ"
            ></activity>
        <activity android:name=".main.CCNATopic"

            android:label="CCNA Topic"
        ></activity>
        <activity android:name=".main.CCNABuy"

            android:label="CCNA Buy"
            ></activity>


    </application>
   <!-- <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
-->
</manifest>
